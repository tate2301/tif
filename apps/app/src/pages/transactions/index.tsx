import OverviewLayout from "@/layouts/OverviewLayout";
import React from "react";

type Props = {};

const Transactions = (props: Props) => {
  return <OverviewLayout heading="Transactions">Transactions</OverviewLayout>;
};

export default Transactions;
